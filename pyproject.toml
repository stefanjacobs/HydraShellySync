[project]
name = "hydrawise"
version = "0.1.0"
description = ""
authors = [{name = "HydraShellySync"}]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.28.1",
    "pyyaml>=6.0",
]
